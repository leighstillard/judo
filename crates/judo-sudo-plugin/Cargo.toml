[package]
name = "judo-sudo-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
judo-proto = { path = "../judo-proto" }
serde_json = { workspace = true }
libc = "0.2"
